## 1. 
```
class base {
    public:
    base(){
        cout<<"base ctor\n";
    }
    ~base(){
        cout<<"base dtor\n";
    }
};
class derived : public base {
    public:
    derived(){
        cout<<"derived ctor\n";
    }
    ~derived(){
        cout<<"derived dtor\n";
    }
};
int main(){
    base *b = new derived();
    return 0;
}
```
output ??

output: 
   base ctor
   derived ctor

why??
```
base *b = new derived(); -> dynamic/heap allocation -> our reponsibility to delete 
```
now:
```
delete b;
```
output??

output: base dtor

not derived dtor: -> If a class is intended to be deleted through a base pointer, its destructor must be virtual.

#### what virtual does:

 ![alt text](<0.00public/image copy 2.png>)

 without virtual:
 .
 .
 .
 .
 ....
 
 #### how memory is allocated to a class and its derived class.
 - what pointer points to?
 - does the derived class makes copeies of parent class?
 - how destructor is called in terms of memory
 - dicisions at late binding
 - what would happen: a function inside parent class is: without virtual and with virtual
    - which function would be called : base or derived?

- virtual -> vtable -> seperate for both parent and child class
                |
                |--> void *ptr ( created -> invisible)
                      |
                can we access it through main()

- can there ba virtual ctor?
- at the initialization list-> vtable is created
- virtual -> use -> late binding
 #### ___FUNCTION__ keyword

 ## Struct vs Class
 difference : converntional
 Struct:
 - data bundle
 - default access modifier: public

 Class:
 - responsibilities
  - default access modifier:  private







